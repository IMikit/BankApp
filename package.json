{
  "name": "react-bank",
  "version": "0.1.0",
  "scripts": {
    "setup": "./setup.sh",
    "start": "concurrently \"npm run ui\" \"npm run api\"",
    "ui": "cd ui && npm start",
    "api": "cd api && npm start"
  },
  "devDependencies": {
    "concurrently": "^3.1.0"
  }
}
